import { IConfig } from './modal.type';
const config: IConfig = {
    style: {
        opacity: 0.7, // 透明度 0.7
    },
    props: {
        close: true, // 显示关闭按钮
        maskClose: true,
    },
};

export default config;
